import { decl } from 'bem-react-core';

export default decl({
    block : 'Menu',
    elem : 'Group'

    // TODO: title
    // attrs({title}) {
    // },

}, {
    propTypes : {
    },

    defaultProps : {
    }
});

